{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug vmm",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/vmm",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "preLaunchTask": "build (debug)"
        }
    ]
}
